return {
    "phaazon/hop.nvim",
    version = 'v2',
    config = function()
        require('hop').setup({ multi_windows = false })
    end,
    keys = {
        {"<leader>w", ":HopWord<CR>", desc = " Hop on word"}
    }
}
