return {
    'nvim-tree/nvim-tree.lua',
    name = "nvim-tree",
    keys = {
        { '<leader>pv', function() vim.cmd.NvimTreeToggle() end, desc = "Toggle nvim tree" }
    },
    config = function()
        require("nvim-tree").setup()
    end,
    cmd = "NvimTreeToggle"
}
